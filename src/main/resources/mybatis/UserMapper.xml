<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tourism.mapper.UserMapper">
	<!--<select id="selectByUserSelective" parameterType="User" resultMap="com.tourism.mapper.BeanMapper.userMap">-->
	<select id="selectByUserSelective" parameterType="User" resultType="User">
		select * from user
		<where>
			<if test="userId != null">
				user_id = #{userId,jdbcType=INTEGER},
			</if>
			<if test="userName != null">
				user_name = #{userName,jdbcType=VARCHAR},
			</if>
			<if test="userCreateTime != null">
				user_create_time = #{userCreateTime,jdbcType=TIMESTAMP},
			</if>
			<if test="userUpdateTime != null">
				user_update_time = #{userUpdateTime,jdbcType=TIMESTAMP},
			</if>
			<if test="userPassword != null">
				user_password = #{userPassword,jdbcType=VARCHAR},
			</if>
		</where>
	</select>
	<!--<select id="selectByUserId" parameterType="int" resultMap="com.tourism.mapper.BeanMapper.userMap">-->
	<select id="selectByUserId" parameterType="int" resultType="User">
		select * from user where user_id = #{userId}
	</select>
</mapper>


